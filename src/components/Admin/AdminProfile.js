import React from "react";
import { Box, Container, Typography } from "@mui/material";

const AdminProfile = () => {
  return (
    <Box sx={{ flexGrow: 1, p: 5 }}>
      <Container fixed>
        <Typography variant="h5">AdminProfile</Typography>
      </Container>
    </Box>
  );
};

export default AdminProfile;
