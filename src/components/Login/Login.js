import React from "react";
import { Box, Container, Typography } from "@mui/material";

const Login = () => {
  return (
    <Box sx={{ flexGrow: 1, p: 5 }}>
      <Container fixed>
        <Typography variant="h5">Login First</Typography>
      </Container>
    </Box>
  );
};

export default Login;
